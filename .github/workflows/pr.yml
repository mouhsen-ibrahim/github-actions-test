name: PR

on:
  pull_request:

jobs:
  prepare-targets:
    runs-on: ubuntu-latest
    steps:
      #- uses: actions/checkout@v3
      - name: test
        env:
          GITHUB_EVENT: ${{ toJson(github.event) }}
        run: echo "${GITHUB_EVENT}"
